import 'package:flutter/material.dart';

class ProjectItem {
  final Color iconColor;
  final String title;
  final int count;

  const ProjectItem({
    required this.iconColor,
    required this.title,
    required this.count,
  });
}