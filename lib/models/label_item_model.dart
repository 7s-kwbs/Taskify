import 'dart:ui';

class LabelItem {
  final Color color;
  final String title;
  final  int count;

  const LabelItem({
    required this.color,
    required this.title,
    required this.count,
  });
}