import 'package:flutter/material.dart';

class MytaskScreen extends StatelessWidget{

  const MytaskScreen({ super.key});

  @override
  Widget build(BuildContext context){
    return Center(
      child: Text("this is my task"),
    );
  }
}